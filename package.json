{
  "name": "wp-site-wwii",
  "version": "1.8.92",
  "description": "⚔ Webpack构建的二战军事资料库网站 ⚔",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "start:pm2": "pm2 start ecosystem.config.js",
    "build:all": "npm run build:index && npm run build:de && npm run build:fr && npm run build:it && npm run build:jp && npm run build:ru && npm run build:uk && npm run build:us",
    "build:index": "export NODE_OPTIONS=--openssl-legacy-provider && webpack --config webpack.build.index.js && export NODE_OPTIONS=",
    "build:de": "export NODE_OPTIONS=--openssl-legacy-provider && webpack --config webpack.build.de.js && export NODE_OPTIONS=",
    "build:fr": "export NODE_OPTIONS=--openssl-legacy-provider && webpack --config webpack.build.fr.js && export NODE_OPTIONS=",
    "build:it": "export NODE_OPTIONS=--openssl-legacy-provider && webpack --config webpack.build.it.js && export NODE_OPTIONS=",
    "build:jp": "export NODE_OPTIONS=--openssl-legacy-provider && webpack --config webpack.build.jp.js && export NODE_OPTIONS=",
    "build:ru": "export NODE_OPTIONS=--openssl-legacy-provider && webpack --config webpack.build.ru.js && export NODE_OPTIONS=",
    "build:uk": "export NODE_OPTIONS=--openssl-legacy-provider && webpack --config webpack.build.uk.js && export NODE_OPTIONS=",
    "build:us": "export NODE_OPTIONS=--openssl-legacy-provider && webpack --config webpack.build.us.js && export NODE_OPTIONS="
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dyslab/wp-site-wwii.git"
  },
  "keywords": [
    "webpack",
    "express",
    "pug"
  ],
  "author": "dyslab",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dyslab/wp-site-wwii/issues"
  },
  "homepage": "https://github.com/dyslab/wp-site-wwii#readme",
  "dependencies": {
    "babel-runtime": "^6.26.0",
    "cookie-parser": "^1.4.4",
    "html-webpack-plugin": "^3.2.0",
    "http-errors": "^1.7.2",
    "pug": "^2.0.4"
  },
  "devDependencies": {
    "@babel/core": "^7.3.3",
    "@babel/plugin-transform-runtime": "^7.4.4",
    "@babel/preset-env": "^7.4.5",
    "acorn": "^6.4.1",
    "apply-loader": "^2.0.0",
    "babel-core": "^6.26.3",
    "babel-loader": "^8.0.5",
    "babel-minify-webpack-plugin": "^0.3.1",
    "babel-plugin-transform-runtime": "^6.23.0",
    "copy-webpack-plugin": "^6.0.3",
    "css-loader": "^2.1.0",
    "express": "^4.17.1",
    "extract-loader": "^3.1.0",
    "file-loader": "^3.0.1",
    "html-loader": "^0.5.5",
    "mini-css-extract-plugin": "^0.5.0",
    "pug-loader": "^2.4.0",
    "style-loader": "^0.23.1",
    "webpack": "^4.47.0",
    "webpack-cli": "^4.10.0"
  }
}
